apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: auto-pe-rw-caller-vuln
on:
  workflow_dispatch:
jobs:
  scan:
    steps:
      - name: Checkout
        uses: cloudbees-io/checkout@v1

      - id: gosec-step
        name: Run gosec scan
        uses: cloudbees-io/gosec-sast-scan@v1
        with:
          output-format: cloudbees

      - id: print-gosec-outputs
        name: Print gosec outputs
        uses: docker://alpine:latest
        run: |
          echo "Gosec outputs:"
          echo "Critical: ${{ steps.gosec-step.outputs.critical-count }}"
          echo "Very high: ${{ steps.gosec-step.outputs.very-high-count }}"
          echo "High: ${{ steps.gosec-step.outputs.high-count }}"
          echo "Medium: ${{ steps.gosec-step.outputs.medium-count }}"
          echo "Low: ${{ steps.gosec-step.outputs.low-count }}"
          echo "Policy Subject: ${{ steps.gosec-step.outputs.policy-subject }}"

  call-reusable:
    needs:
      - scan
    uses: .cloudbees/workflows/auto-pe-reusable-wf.yaml

  independent-job:
    steps:
      - name: Independent step
        uses: docker://alpine:latest
        shell: sh
        run: echo "This job must succeed even when call-reusable is blocked"

