  apiVersion: automation.cloudbees.io/v1alpha1
  kind: workflow
  name: auto-pe-rw-caller-vuln
  on:
    workflow_dispatch: {}
  jobs:
    call-reusable:
      uses: ./.cloudbees/workflows/auto-pe-reusable-wf.yaml
    independent-job:
      steps:
        - name: Run
          uses: docker://alpine:3.19
          run: echo "independent job"
