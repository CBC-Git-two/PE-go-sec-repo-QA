apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: auto-pe-reusable-wf
on:
  workflow_call:
jobs:
  run:
    steps:
      - name: Placeholder step
        uses: docker://alpine:3.19
        shell: sh
        run: echo "Reusable workflow — policy inheritance target"
